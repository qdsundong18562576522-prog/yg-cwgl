# 扬光科技财务管理系统 — 第一期实施计划

## 项目背景

为中大型系统集成公司（10-50人）开发内部财务管理系统，替代当前Excel手工管理模式。现有项目管理系统 yg-xmgl-new 已完成采购链模块，本系统第一期独立运行、后续对接。

**技术栈**: React 19 + TypeScript + Ant Design + Vite（前端）| NestJS + Prisma + PostgreSQL（后端）

---

## 第一期范围（本期开发）

### A. 基础数据
| 模块 | 内容 |
|------|------|
| 会计科目 | 科目编码、名称、类型（资产/负债/权益/收入/费用）、层级树、启用/停用 |
| 银行账户 | 开户行、账号、账户类型（基本户/一般户/专户）、初始余额、启用/停用 |
| 往来单位 | 客户/供应商名称、联系人、电话、地址、类型（客户/供应商/两者） |

### B. 内部对账（核心模块）
| 子模块 | 功能点 |
|--------|--------|
| **银行流水** | Excel/CSV 导入（模板下载）、手动录入、查询筛选、编辑删除 |
| **内部流水** | 收付款记录、费用记录、Excel 导入、分类筛选 |
| **银行对账** | 自动匹配（金额+日期+摘要）、手工核销、未达账项标记、**余额调节表** |
| **往来对账** | 应收管理（按客户+项目）、应付管理（按供应商+项目）、**账龄分析**、核销 |
| **对账报告** | 余额调节表、往来对账明细、对账汇总导出 Excel |

### C. 资金管理（基础版）
| 子模块 | 功能点 |
|--------|--------|
| 资金看板 | 总览卡片（总余额/月收入/月支出）、各账户余额、近期流水 |
| 资金流水 | 全账户流水总览、按账户/日期/方向筛选 |
| 资金日报 | 每日自动汇总各账户余额和发生额、打印/导出 |
| 项目资金 | 按项目归集资金流入/流出（手动关联项目） |

### D. 系统管理
| 模块 | 内容 |
|------|------|
| 用户管理 | 登录/登出、JWT、增删改查、启用/停用（参照 yg-xmgl-new 模式） |
| 角色权限 | admin（全部）、finance（财务操作）、leader（查看审批）、viewer（只读） |
| 操作日志 | 关键操作记录（导入、对账、核销、删除） |

---

## 数据库核心模型（Prisma）

```
Account               银行/现金账户
  ├── id, name, accountNo, bankName, type(BANK/CASH), balance, currency, status
  └── BankStatement ← 对账单批次
        └── BankStatementItem  银行流水条目（金额、日期、摘要、对方、匹配状态）
              └── matchedTransaction → Transaction

Transaction           内部流水（收/付款记录）
  ├── id, type(INCOME/EXPENSE/TRANSFER), amount, date, description
  ├── counterpartyId → Counterparty
  ├── accountId → Account
  ├── projectId (optional，后续对接yg-xmgl-new)
  └── reconciliationStatus(匹配/未匹配/部分匹配)

Counterparty        往来单位（客户/供应商）
  ├── id, name, type(CUSTOMER/SUPPLIER/BOTH), contact, phone, address
  ├── Receivable ← 应收
  │     └── amount, dueDate, status, projectId
  └── Payable ← 应付
        └── amount, dueDate, status, projectId

Reconciliation      对账记录
  └── period, accountId, bankBalance, bookBalance, difference, status

ChartOfAccount      会计科目
  └── code, name, type, parentId(树形), level, isActive

Approval            审批记录 (对账确认/付款审批)
  └── entityType, entityId, status, applicantId, approverId, comment

User                用户 (参照yg-xmgl-new)
  └── role(admin/finance/leader/viewer)
```

---

## 项目结构

```
yg-cwgl/
├── frontend/                # React + Vite + Ant Design
│   └── src/
│       ├── layouts/         # MainLayout (参照yg-xmgl-new)
│       ├── components/      # 共享组件 (ImportModal, SearchForm, etc.)
│       ├── pages/
│       │   ├── accounts/    # 银行账户管理
│       │   ├── subjects/    # 会计科目
│       │   ├── counterparties/ # 往来单位
│       │   ├── bank/        # 银行流水+对账
│       │   ├── internal/    # 内部流水
│       │   ├── reconciliation/ # 对账管理+余额调节表
│       │   ├── ar-ap/       # 应收应付+账龄分析
│       │   ├── fund/        # 资金看板+日报+项目资金
│       │   ├── reports/     # 对账报告
│       │   ├── users/       # 用户管理
│       │   └── login/       # 登录
│       ├── stores/          # Zustand (authStore, etc.)
│       ├── api/             # Axios API 封装
│       └── App.tsx          # 路由配置
│
├── backend/                 # NestJS + Prisma
│   ├── prisma/
│   │   └── schema.prisma    # 完整数据模型
│   ├── src/
│   │   ├── auth/            # JWT 认证 (参照yg-xmgl-new)
│   │   ├── users/           # 用户管理
│   │   ├── accounts/        # 银行账户 CRUD
│   │   ├── subjects/        # 会计科目 CRUD
│   │   ├── counterparties/  # 往来单位 CRUD
│   │   ├── transactions/    # 内部流水
│   │   ├── bank-statements/ # 银行流水导入+管理
│   │   ├── reconciliation/  # 对账引擎
│   │   ├── receivables/     # 应收管理
│   │   ├── payables/        # 应付管理
│   │   ├── fund/            # 资金看板+日报
│   │   ├── reports/         # 报表
│   │   ├── approval/        # 审批记录
│   │   ├── common/          # 工具类 (code-generator, guards, decorators)
│   │   └── prisma/          # PrismaModule + PrismaService
│   └── uploads/             # 文件上传目录
```

---

## 实现步骤

### Step 1: 项目脚手架
- 初始化 NestJS 项目（`nest new backend`）
- 初始化 Vite + React 项目（`npm create vite frontend`）
- 配置 Prisma + PostgreSQL 数据源
- 复制 yg-xmgl-new 的公共工具（JWT策略、PrismaModule、Guards、Decorators、code-generator）
- 配置前端路由 + Ant Design + TanStack Query + Zustand

### Step 2: 基础数据模块
- 会计科目 CRUD（树形结构、层级管理）
- 银行账户 CRUD
- 往来单位 CRUD
- 对应前端页面

### Step 3: 用户认证 + 系统管理
- 登录/注册/JWT认证
- 用户管理（CRUD、角色）
- 角色权限守卫（admin/finance/leader/viewer）
- 操作日志记录

### Step 4: 银行流水 + 内部流水
- Excel/CSV 导入模板 + 解析服务（使用 ExcelJS/xlsx 库）
- 银行流水列表、筛选、编辑
- 内部收/付款记录 CRUD
- 内部流水导入

### Step 5: 对账引擎（核心）
- 自动匹配算法（金额精确匹配 + 日期容差 + 摘要关键词）
- 手工核销 UI（左右两栏对比、拖拽/点击匹配）
- 未达账项管理
- 余额调节表自动生成

### Step 6: 应收应付管理
- 应收账款列表（按客户、项目）
- 应付账款列表（按供应商、项目）
- 核销操作
- 账龄分析图（Ant Design Charts）

### Step 7: 资金看板 + 资金日报
- 资金总览 Dashboard（KPI 卡片、账户余额环形图）
- 资金流水总览
- 资金日报自动生成 + 导出
- 项目资金归集视图

### Step 8: 对账报告
- 余额调节表 PDF/Excel 导出
- 往来对账明细表
- 对账汇总报告

### Step 9: 集成测试 + 部署
- 端到端流程测试
- Docker 部署配置
- 生产环境部署

---

## 关键设计决策

1. **自动对账策略**: 优先金额+日期精确匹配，辅以摘要关键词相似度，未匹配项标记人工处理
2. **导入格式**: 提供标准 Excel 模板下载，支持主流银行导出的 CSV 格式映射
3. **与 yg-xmgl-new 的关系**: 第一期完全独立，但预留 projectId 字段，第二期通过 API 网关或共享数据库进行对接
4. **审批流**: 第一期仅记录审批操作（对账确认、大额付款标记），第二期再做完整审批工作流
5. **自动编号**: 沿用 yg-xmgl-new 的 code-generator 模式，例如 `DZ-{日期}-{流水号}`（对账单）、`FK-{日期}-{流水号}`（付款单）

---

## 验证方式

1. **本地开发**: `npm run start:dev` 启动后端 + `npm run dev` 启动前端
2. **核心流程测试**:
   - 导入银行流水 → 创建内部流水 → 执行自动对账 → 查看余额调节表
   - 创建应收/应付 → 执行核销 → 查看账龄分析
   - 多账户场景 → 资金看板正确汇总各账户余额
3. **权限测试**: 不同角色登录验证菜单和操作权限
4. **数据验证**: 导入真实 Excel 数据验证对账准确率
